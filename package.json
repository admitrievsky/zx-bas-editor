{
  "name": "zx-bas-editor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "bash scripts/deploy-pages.sh",
    "test": "node --experimental-strip-types test/units.ts",
    "test:tap": "node --experimental-strip-types test/roundtrip.ts"
  },
  "dependencies": {
    "zmakebas": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "license": "GPL-3.0-or-later"
}
